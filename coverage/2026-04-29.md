# Coverage report — 2026-04-29

Auto-generated by `scripts/coverage-report.ts`. Re-run with
`npm run coverage` after every catalog change.

## Summary

- **18 tags** total
- **2 tags ready** (≥5 cmds, ≥3 phases, ≥1 tagged tool, ≥1 MITRE mapping)
- **8 tags placeholder** (0-2 commands)
- **129 commands** across 29 steps in 5 phases
- **27% overall MITRE coverage** (35 / 129 commands carry `mitreTechniques`)

The roadmap targets ≥75% MITRE coverage on non-recon commands. Currently
the bar is **27%** site-wide.

## Per-tag

Sorted by command count descending. The "ready" column reflects the
ROADMAP appendix bar; tags marked ✗ have a gap list in the rightmost
column.

| tag | group | cmds | tools | phases | mitre % | ready | gaps |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `kerberos` | Identity / auth | 16 | 5 | 3/5 | 63% | ✓ | — |
| `ldap` | Identity / auth | 11 | 5 | 3/5 | 45% | ✓ | — |
| `apache` | Web server | 7 | 0 | 3/5 | 0% | ✗ | no tagged tool (map can't derive discoverer); no MITRE mapping |
| `wordpress` | Framework / CMS | 7 | 2 | 2/5 | 0% | ✗ | 2/5 phases (need ≥3); no MITRE mapping |
| `nginx` | Web server | 5 | 0 | 2/5 | 0% | ✗ | 2/5 phases (need ≥3); no tagged tool (map can't derive discoverer); no MITRE mapping |
| `iis` | Web server | 4 | 0 | 2/5 | 0% | ✗ | only 4 cmds (need ≥5); 2/5 phases (need ≥3); no tagged tool (map can't derive discoverer); no MITRE mapping |
| `node` | Language / runtime | 3 | 0 | 1/5 | 0% | ✗ | only 3 cmds (need ≥5); 1/5 phases (need ≥3); no tagged tool (map can't derive discoverer); no MITRE mapping |
| `aws` | Cloud / orchestration | 3 | 3 | 2/5 | 0% | ✗ | only 3 cmds (need ≥5); 2/5 phases (need ≥3); no MITRE mapping |
| `gcp` | Cloud / orchestration | 3 | 2 | 2/5 | 0% | ✗ | only 3 cmds (need ≥5); 2/5 phases (need ≥3); no MITRE mapping |
| `azure` | Cloud / orchestration | 3 | 2 | 2/5 | 0% | ✗ | only 3 cmds (need ≥5); 2/5 phases (need ≥3); no MITRE mapping |
| `postgres` | Database | 2 | 0 | 2/5 | 0% | ✗ | only 2 cmds (need ≥5); 2/5 phases (need ≥3); no tagged tool (map can't derive discoverer); no MITRE mapping |
| `mysql` | Database | 2 | 0 | 2/5 | 0% | ✗ | only 2 cmds (need ≥5); 2/5 phases (need ≥3); no tagged tool (map can't derive discoverer); no MITRE mapping |
| `k8s` | Cloud / orchestration | 2 | 2 | 2/5 | 0% | ✗ | only 2 cmds (need ≥5); 2/5 phases (need ≥3); no MITRE mapping |
| `spring` | Framework / CMS | 1 | 0 | 1/5 | 0% | ✗ | only 1 cmd (need ≥5); 1/5 phases (need ≥3); no tagged tool (map can't derive discoverer); no MITRE mapping |
| `express` | Framework / CMS | 1 | 0 | 1/5 | 0% | ✗ | only 1 cmd (need ≥5); 1/5 phases (need ≥3); no tagged tool (map can't derive discoverer); no MITRE mapping |
| `mssql` | Database | 1 | 0 | 1/5 | 0% | ✗ | only 1 cmd (need ≥5); 1/5 phases (need ≥3); no tagged tool (map can't derive discoverer); no MITRE mapping |
| `java` | Language / runtime | 0 | 1 | 0/5 | 0% | ✗ | only 0 cmds (need ≥5); 0/5 phases (need ≥3); no MITRE mapping |
| `dotnet` | Language / runtime | 0 | 1 | 0/5 | 0% | ✗ | only 0 cmds (need ≥5); 0/5 phases (need ≥3); no MITRE mapping |

## Per-phase

| phase | steps | cmds (total) | tag-agnostic | tagged | tools | mitre % |
| --- | --- | --- | --- | --- | --- | --- |
| **Recon** (`recon`) | 7 | 42 | 17 | 25 | 23 | 12% |
| **Vulns** (`vuln`) | 6 | 35 | 12 | 23 | 17 | 11% |
| **Exploit** (`exploit`) | 5 | 11 | 10 | 1 | 13 | 18% |
| **Post-ex** (`post-ex`) | 6 | 36 | 20 | 16 | 25 | 67% |
| **Defense** (`defense`) | 5 | 5 | 5 | 0 | 11 | 0% |

## What to write next

The roadmap's M1 step says: pick the **next 6-10 tags to deepen**.
Candidates (sorted by impact: which gaps would meaningfully help a
real engagement):

1. **`apache`** (Web server) — no tagged tool (map can't derive discoverer); no MITRE mapping
2. **`wordpress`** (Framework / CMS) — 2/5 phases (need ≥3); no MITRE mapping
3. **`nginx`** (Web server) — 2/5 phases (need ≥3); no tagged tool (map can't derive discoverer); no MITRE mapping
4. **`iis`** (Web server) — only 4 cmds (need ≥5); 2/5 phases (need ≥3); no tagged tool (map can't derive discoverer); no MITRE mapping
5. **`node`** (Language / runtime) — only 3 cmds (need ≥5); 1/5 phases (need ≥3); no tagged tool (map can't derive discoverer); no MITRE mapping
6. **`aws`** (Cloud / orchestration) — only 3 cmds (need ≥5); 2/5 phases (need ≥3); no MITRE mapping
7. **`gcp`** (Cloud / orchestration) — only 3 cmds (need ≥5); 2/5 phases (need ≥3); no MITRE mapping
8. **`azure`** (Cloud / orchestration) — only 3 cmds (need ≥5); 2/5 phases (need ≥3); no MITRE mapping
9. **`postgres`** (Database) — only 2 cmds (need ≥5); 2/5 phases (need ≥3); no tagged tool (map can't derive discoverer); no MITRE mapping
10. **`mysql`** (Database) — only 2 cmds (need ≥5); 2/5 phases (need ≥3); no tagged tool (map can't derive discoverer); no MITRE mapping

---

Re-run: `npm run coverage`
